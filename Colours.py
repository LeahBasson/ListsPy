# Create a list of five different colours.
# Print the first element (at index 0) of the list.
# Print the last element of the list (without explicitly counting the elements).

colours = ["red", "green", "blue", "yellow", "black"]

print(colours[0])
print(colours[-1])